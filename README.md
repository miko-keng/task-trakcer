# 🚀 Angular Task Tracker

A professional-grade Task Management application built to demonstrate core Angular concepts, including **Standalone Components**, **Two-Way Data Binding**, and **Local Storage Persistence**.

## ✨ Features
* **Full CRUD:** Add, toggle, and delete tasks.
* **Data Persistence:** Uses Browser LocalStorage so tasks remain after refresh.
* **Modern UI:** Clean, responsive design with "Empty State" handling.
* **Type Safety:** Built with strict TypeScript interfaces.

## 🛠️ Tech Stack
* **Framework:** Angular 19+
* **Language:** TypeScript
* **Styling:** Scoped CSS

## 🏁 Getting Started
1. Clone the repo: `git clone https://github.com/jessiekeng/task-trakcer.git`
2. Install dependencies: `npm install`
3. Run the app: `ng serve`
